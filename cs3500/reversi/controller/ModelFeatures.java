package cs3500.reversi.controller;

/**
 * The model features interface. The only method that exists is checking whether it is the
 * players turn. (The player being the player that the controller is functioning for).
 */
public interface ModelFeatures {
  void yourTurn();
}
